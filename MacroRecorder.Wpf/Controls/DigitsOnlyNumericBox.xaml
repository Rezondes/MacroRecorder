<UserControl x:Class="MacroRecorder.Wpf.Controls.DigitsOnlyNumericBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             MinHeight="40">
    <UserControl.Resources>
        <Style x:Key="UiNumericSpinnerRepeatButton" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource UiBrush.SurfaceHover}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource UiBrush.SurfaceSelected}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="UiNumericSpinnerArrowPath" TargetType="Path">
            <Setter Property="Stroke" Value="{DynamicResource UiBrush.TextSecondary}" />
            <Setter Property="StrokeThickness" Value="1.6" />
            <Setter Property="StrokeLineJoin" Value="Round" />
            <Setter Property="Stretch" Value="None" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=RepeatButton}}"
                             Value="True">
                    <Setter Property="Stroke" Value="{DynamicResource UiBrush.TextPrimary}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPressed, RelativeSource={RelativeSource AncestorType=RepeatButton}}"
                             Value="True">
                    <Setter Property="Stroke" Value="{DynamicResource UiBrush.Primary}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border x:Name="ChromeBorder"
            SnapsToDevicePixels="True"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Background>
            <DynamicResource ResourceKey="UiBrush.SurfaceElevated" />
        </Border.Background>
        <Border.BorderBrush>
            <DynamicResource ResourceKey="UiBrush.Border" />
        </Border.BorderBrush>
        <Grid MinHeight="{Binding MinInnerHeight, ElementName=Root}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PART_TextBox"
                     Grid.Column="0"
                     Padding="10,8"
                     BorderThickness="0"
                     Background="Transparent"
                     VerticalContentAlignment="Center">
                <TextBox.Foreground>
                    <DynamicResource ResourceKey="UiBrush.TextPrimary" />
                </TextBox.Foreground>
                <TextBox.CaretBrush>
                    <DynamicResource ResourceKey="UiBrush.TextPrimary" />
                </TextBox.CaretBrush>
                <TextBox.SelectionBrush>
                    <DynamicResource ResourceKey="UiBrush.SurfaceSelected" />
                </TextBox.SelectionBrush>
                <TextBox.SelectionTextBrush>
                    <DynamicResource ResourceKey="UiBrush.TextPrimary" />
                </TextBox.SelectionTextBrush>
            </TextBox>
            <Grid x:Name="SpinnerPanel"
                  Grid.Column="1"
                  Width="34"
                  Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <RepeatButton x:Name="SpinnerUpButton"
                              Grid.Row="0"
                              Delay="400"
                              Interval="80"
                              Style="{StaticResource UiNumericSpinnerRepeatButton}"
                              Cursor="Hand"
                              Click="SpinnerUpButton_OnClick">
                    <Path Data="M 0 7 L 7 1 L 14 7"
                          Style="{StaticResource UiNumericSpinnerArrowPath}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />
                </RepeatButton>
                <Border Grid.Row="1"
                        Height="1"
                        Background="{DynamicResource UiBrush.Border}" />
                <RepeatButton x:Name="SpinnerDownButton"
                              Grid.Row="2"
                              Delay="400"
                              Interval="80"
                              Style="{StaticResource UiNumericSpinnerRepeatButton}"
                              Cursor="Hand"
                              Click="SpinnerDownButton_OnClick">
                    <Path Data="M 0 1 L 7 7 L 14 1"
                          Style="{StaticResource UiNumericSpinnerArrowPath}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />
                </RepeatButton>
            </Grid>
        </Grid>
    </Border>
</UserControl>
