<UserControl x:Class="MacroRecorder.App.Views.Editor.InsertMouseClickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:MacroRecorder.App.Localization"
             xmlns:wpf="clr-namespace:MacroRecorder.Wpf.Controls;assembly=MacroRecorder.Wpf"
             Background="Transparent"
             Focusable="True">
    <Border Style="{DynamicResource UiDialogSurface}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Style="{DynamicResource UiDialogTitle}"
                       Margin="0,0,0,4"
                       Text="{loc:Loc DialogInsertMouse_Heading}" />
            <TextBlock Grid.Row="1"
                       Style="{DynamicResource UiDialogDescription}"
                       Margin="0,0,0,16"
                       Text="{loc:Loc DialogInsertMouse_Description}" />
            <ComboBox x:Name="ButtonCombo"
                      Grid.Row="2"
                      Margin="0,0,0,12"
                      Style="{DynamicResource UiSettingsComboBox}" />
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Style="{DynamicResource UiFormFieldLabel}"
                               Text="{loc:Loc DialogInsertMouse_LabelX}" />
                    <wpf:DigitsOnlyNumericBox x:Name="XBox"
                                             MinHeight="36"
                                             MinInnerHeight="36"
                                             InputFontSize="13"
                                             DigitsOnly="True"
                                             ShowSpinner="False" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Style="{DynamicResource UiFormFieldLabel}"
                               Text="{loc:Loc DialogInsertMouse_LabelY}" />
                    <wpf:DigitsOnlyNumericBox x:Name="YBox"
                                             MinHeight="36"
                                             MinInnerHeight="36"
                                             InputFontSize="13"
                                             DigitsOnly="True"
                                             ShowSpinner="False" />
                </StackPanel>
            </Grid>
            <StackPanel Grid.Row="4"
                        Style="{DynamicResource UiDialogFooter}">
                <Button Content="{loc:Loc Common_Cancel}"
                        MinWidth="100"
                        Margin="0,0,8,0"
                        Padding="12,6"
                        Style="{DynamicResource UiButtonChrome}"
                        Click="OnCancelClick" />
                <Button Content="{loc:Loc Common_Ok}"
                        MinWidth="100"
                        Padding="12,6"
                        IsDefault="True"
                        Style="{DynamicResource UiPrimaryButton}"
                        Click="OnOkClick" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
