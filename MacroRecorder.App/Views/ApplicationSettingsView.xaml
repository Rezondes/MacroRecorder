<UserControl x:Class="MacroRecorder.App.Views.ApplicationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:MacroRecorder.App.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="380">
    <Border Margin="20" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                <Button Content="{loc:Loc Common_Cancel}"
                        Width="96"
                        Margin="0,0,8,0"
                        Padding="10,6"
                        IsCancel="True"
                        Click="OnCancel" />
                <Button Content="{loc:Loc Main_Settings_Save}"
                        Width="96"
                        Padding="10,6"
                        IsDefault="True"
                        Click="OnSave" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="{loc:Loc Main_Settings_LanguageHeader}"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#1A1A1A"
                           Margin="0,0,0,8" />
                <ComboBox x:Name="LanguageCombo"
                          MinHeight="36"
                          FontSize="13"
                          Padding="8,4"
                          VerticalContentAlignment="Center" />
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
